# Bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Optional: register commands instantly to one server instead of globally
GUILD_ID=

# Channel where /vouch embeds are posted
VOUCH_CHANNEL_ID=1550871579087609886

# Channel where every message gets an auto ✅ reaction
AUTO_REACT_CHANNEL_ID=1550871609588457544
AUTO_REACT_EMOJI=✅

# Role pinged during the /payment flow (Stripe/Giftcard requests, "I've Paid")
PAYMENT_PING_ROLE_ID=1550868852911644794

# Roles that get a user kicked if they ping them (comma-separated)
PROTECTED_ROLE_IDS=1550868852911644794,1550868762176004099

# Optional: shown when a user picks Cashapp
CASHAPP_TAG=$yourcashtag

# Verification (OAuth2) — from Developer Portal → OAuth2 → Client Secret
CLIENT_SECRET=your_client_secret
# Public URL where the bot is hosted (no trailing slash). Add PUBLIC_URL/callback as a redirect in the portal.
PUBLIC_URL=https://your-bot.up.railway.app
VERIFIED_ROLE_ID=
UNVERIFIED_ROLE_ID=
VERIFY_CHANNEL_ID=

# Optional: Postgres URL so verified users persist across redeploys (otherwise data.json is used)
DATABASE_URL=

# Optional
EMBED_COLOR=2b2d31
PORT=3000

# Botivo ticket bot (runs in the same process when set; see ticket-bot/.env.example for its other vars)
TICKET_BOT_TOKEN=
SUPPORT_ROLE_ID=
TICKET_CATEGORY_ID=
TICKET_LOG_CHANNEL_ID=
PANEL_CHANNEL_ID=
WEBSITE_URL=
STRIPE_SECRET_KEY=
# Stripe webhook signing secret for order logging (endpoint: https://<bot-host>/stripe/webhook)
STRIPE_WEBHOOK_SECRET=
# Channel that receives order embeds
ORDER_CHANNEL_ID=1551324617896099980
